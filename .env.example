# Public browser configuration. Leave the origin empty to use the same-origin proxy.
VITE_API_ORIGIN=
VITE_API_BASE_PATH=/api/v1

# Development proxy only; this value is not exposed to the browser.
API_PROXY_TARGET=http://127.0.0.1:8000
